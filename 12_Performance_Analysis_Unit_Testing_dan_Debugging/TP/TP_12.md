# Tugas Pendahuluan 12: Performance Analysis, Unit Testing, dan Debugging

**Nama:** Surya Pradipta  
**NIM:** 103122400061  
**Kelas:** SE-08-02

## Tugas

Cobalah untuk menangkap kecacatan dalam kode ini

```js
function main() {
  const data = [
    "123",
    456,
    "hello",
    78.9,
    true,
  ];

  for (let i = 0; i < data.length; i++) {
    const result = processData(data[i]);
    console.log(`Item ${i + 1}: ${data[i]} -> ${result}`);
  }
}

function processData(data) {
  const str = data.toLowerCase();
  const num = parseInt(str);
  if (!isNaN(num) && str === String(num)) {
    return `Number: ${num * 2}`;
  }
  return `Teks: ${str} (panjangnya: ${str.length})`;
}

main();
```

## Program/Kode

Tersedia di [index.js](./index.js)

## Output

![Gambar Jawaban](./Screenshot%202026-05-20%20062956.png "Gambar Jawaban")

## Deskripsi

Kecacatan utama pada kode terdapat di fungsi `processData()`, khususnya pada penggunaan `data.toLowerCase()` karena method tersebut hanya bisa digunakan pada tipe string, sedangkan array `data` juga berisi number dan boolean. Selain itu, penggunaan `parseInt()` menyebabkan angka desimal seperti `78.9` tidak diproses dengan benar karena hanya mengambil bagian integer nya saja.
